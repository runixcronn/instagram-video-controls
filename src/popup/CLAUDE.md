# Popup UI Context

Chrome Extension Action Popup (settings & shortcut cheatsheet).

## Files
- `popup.html` : Clean markup for settings toggles and shortcut table.
- `popup.css` : Dark aesthetic matching Instagram's web styling.
- `popup.js` : Syncs with `chrome.storage.sync` to read and update preferences.
